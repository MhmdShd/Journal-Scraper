
Python SHOULD be installed!

install all required libraries using command prompt: "pip install -r requirements.txt"


Main_application.exe -- Main executable file

src/Main_application.py -- Main app with UI for Keywords scanning or Local Files update.

src/Scanner.py -- scans the local files pdfs for provided keywords

src/Scraper.py -- scrapes a chosen website within the UI.

update-pdfs.txt -- for future PDF files updates.

wendriver_functions -- functions archive using selenium.

Pdf list per site -- Is a local folder for PDFs in all sites.

scan_result.txt -- Result of Keyword scan is written here.
